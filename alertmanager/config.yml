global:
    resolve_timeout: 1m
    
route:
  # When a new group of alerts is created by an incoming alert, wait at
  # least 'group_wait' to send the initial notification.
  # This way ensures that you get multiple alerts for the same group that start
  # firing shortly after another are batched together on the first
  # notification.
  group_wait: 10s

  # When the first notification was sent, wait 'group_interval' to send a batch
  # of new alerts that started firing for that group.
  group_interval: 5m

  # If an alert has successfully been sent, wait 'repeat_interval' to
  # resend them.
  repeat_interval: 30m

  # A default receiver
  receiver: "email"

  # All the above attributes are inherited by all child routes and can
  # overwritten on each.
  routes:
    - receiver: "slack"
      group_wait: 10s
      match_re:
        severity: critical|warning
      continue: true

receivers: 
- name: 'email'  
  email_configs:
  - to: shanumodi9@gmail.com
    from: shanumodi9@gmail.com
    smarthost: smtp.gmail.com:587
    auth_username: shanumodi9@gmail.com
    auth_identity: shanumodi9@gmail.com
    auth_password: ${{ secrets.AUTH_PASSWORD }}
    send_resolved: true

- name: 'slack'
  slack_configs:
  - api_url: 'https://hooks.slack.com/services/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'
    channel: '#test123'
    send_resolved: true
